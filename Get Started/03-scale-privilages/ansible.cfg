[defaults]
inventory   = ./inventory 
remote_user = admin 
ask_pass    = false


## if have an error write sudo privilage in /etc/sudoers.d/ansible-users
### admin   ALL=(ALL)  NOPASSWD:ALL
[privilage_escalation]
become          = true
become_method   = sudo 
become_user     = root
become_ask_pass = false

